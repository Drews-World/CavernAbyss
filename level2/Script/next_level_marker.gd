extends Area2D

#@export var next_scene = 
var player = Global.playerBody

#func _on_body_entered(body: Node2D) -> void:
#    if body is Player:  door
#        get_tree().change_scene_to_file(next_scene)
